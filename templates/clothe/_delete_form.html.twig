<form method="post" action="{{ path('app_clothe_delete', {'id': clothe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clothe.id) }}">
    <button class="btn">Delete</button>
</form>
