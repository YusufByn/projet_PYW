{% extends 'base.html.twig' %}

{% block title %}Ajouter un Vêtement - PYW{% endblock %}

{% block body %}
<div class="fade-in-up">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-pink">
            <i class="fas fa-plus-circle me-2"></i>Ajouter un Vêtement
        </h1>
        <a href="{{ path('app_clothe_index') }}" class="btn btn-outline-primary">
            <i class="fas fa-arrow-left me-1"></i>Retour à la liste
        </a>
    </div>

    <!-- Formulaire -->
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-tshirt me-2"></i>Informations du vêtement
                    </h5>
                </div>
                <div class="card-body">
                    {{ include('clothe/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
