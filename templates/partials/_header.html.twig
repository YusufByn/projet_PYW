<!-- Header Desktop -->
<header class="bg-primary text-white py-3 d-none d-lg-block">
    <div class="container">
        <nav class="navbar navbar-expand-lg">
            <a class="navbar-brand text-white fw-bold" href="{{ path('app_clothe_index') }}">
                PYW
            </a>
            
            <div class="navbar-nav ms-auto">
                <a class="nav-link text-white me-3" href="{{ path('app_clothe_index') }}">
                    <i class="fas fa-home"></i> Accueil
                </a>
                
                <a class="nav-link text-white me-3" href="{{ path('app_clothe_new') }}">
                    <i class="fas fa-plus"></i> Ajouter un vêtement
                </a>
                
                {% if app.user %}
                    <a class="nav-link text-white" href="#">
                        <i class="fas fa-user"></i> Profil
                    </a>
                {% else %}
                    <a class="nav-link text-white" href="{{ path('app_login') }}">
                        <i class="fas fa-sign-in-alt"></i> Connexion
                    </a>
                {% endif %}
            </div>
        </nav>
    </div>
</header>

<!-- Navigation Mobile - Fixed Bottom -->
<nav class="navbar fixed-bottom bg-primary text-white d-lg-none">
    <div class="container-fluid">
        <div class="row w-100">
            <div class="col-4 text-center">
                <a class="nav-link text-white d-flex flex-column align-items-center" href="{{ path('app_clothe_index') }}">
                    <i class="fas fa-home fs-4"></i>
                    <small>Accueil</small>
                </a>
            </div>
            <div class="col-4 text-center">
                <a class="nav-link text-white d-flex flex-column align-items-center" href="{{ path('app_clothe_new') }}">
                    <i class="fas fa-plus fs-4"></i>
                    <small>Ajouter</small>
                </a>
            </div>
            <div class="col-4 text-center">
                {% if app.user %}
                    <a class="nav-link text-white d-flex flex-column align-items-center" href="#">
                        <i class="fas fa-user fs-4"></i>
                        <small>Profil</small>
                    </a>
                {% else %}
                    <a class="nav-link text-white d-flex flex-column align-items-center" href="{{ path('app_login') }}">
                        <i class="fas fa-sign-in-alt fs-4"></i>
                        <small>Connexion</small>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
